@model Pecus.Libs.Mail.Templates.Models.AgendaInvitationEmailModel
@{
    Layout = "_Layout.html.cshtml";
}

<h2>予定に招待されました</h2>

<p>@Model.InvitedByName さんから以下の予定に招待されました。</p>

<p><strong>@Model.AgendaTitle</strong></p>

<ul>
    <li>日時: @if (Model.IsAllDay)
        {
            <span>@Model.StartAt.ToString("yyyy/MM/dd") (終日)</span>
        }
        else
        {
            <span>@Model.StartAt.ToString("yyyy/MM/dd HH:mm") - @Model.EndAt.ToString("HH:mm")</span>
        }
    </li>
    @if (!string.IsNullOrWhiteSpace(Model.RecurrenceDescription))
    {
        <li>繰り返し: @Model.RecurrenceDescription</li>
    }
    @if (!string.IsNullOrWhiteSpace(Model.Location))
    {
        <li>場所: @Model.Location</li>
    }
    @if (!string.IsNullOrWhiteSpace(Model.Url))
    {
        <li>URL: <a href="@Model.Url">@Model.Url</a></li>
    }
    <li>組織: @Model.OrganizationName</li>
</ul>

@if (!string.IsNullOrWhiteSpace(Model.Description))
{
    <div style="margin: 16px 0; padding: 12px; background-color: #f5f5f5; border-radius: 4px;">
        <strong>詳細:</strong><br />
        @Model.Description
    </div>
}

<div style="text-align:center; margin:20px 0;">
    <a href="@Model.AgendaUrl" class="button">予定を確認</a>
</div>
