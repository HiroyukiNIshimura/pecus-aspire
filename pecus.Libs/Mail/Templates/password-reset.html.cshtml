@model Pecus.Libs.Mail.Templates.Models.PasswordResetEmailModel
@{
    Layout = "_Layout.html.cshtml";
}

<p>@Model.UserName 様</p>

<p>パスワードリセットのリクエストを受け付けました。</p>

<p>パスワードリセットURL: <a href="@Model.PasswordResetUrl">@Model.PasswordResetUrl</a></p>

<div class="warning">
    <strong>注意：</strong> このリンクは @Model.TokenExpiresAt.ToString("yyyy年MM月dd日 HH:mm") まで有効です。
</div>

<p>このリクエストに心当たりがない場合は、このメールを無視してください。</p>

<p>リクエスト日時: @Model.RequestedAt.ToString("yyyy年MM月dd日 HH:mm:ss")</p>
