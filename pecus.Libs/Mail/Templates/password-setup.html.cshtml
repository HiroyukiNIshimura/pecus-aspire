@model Pecus.Libs.Mail.Templates.Models.PasswordSetupEmailModel
@layout "_Layout.html.cshtml"

<h2>@Model.UserName さん、アカウントが作成されました</h2>

<p>@Model.OrganizationName の管理者によって、あなたのアカウントが作成されました。</p>

<p>アカウントを使用するには、まずパスワードを設定する必要があります。以下のボタンから設定してください。</p>

<div style="text-align:center;">
    <a href="@Model.PasswordSetupUrl" class="button" style="background:#2196F3;">パスワードを設定する</a>
</div>

<div class="warning">
    <strong>重要:</strong> このリンクは @Model.TokenExpiresAt.ToString("yyyy年MM月dd日 HH:mm") まで有効です。
</div>

<p><strong>アカウント情報:</strong></p>
<ul>
    <li>ユーザー名: @Model.UserName</li>
    <li>メールアドレス: @Model.Email</li>
    <li>組織: @Model.OrganizationName</li>
    <li>作成日: @Model.CreatedAt.ToString("yyyy年MM月dd日")</li>
</ul>

<p style="font-size:14px; color:#666;">このメールに心当たりがない場合は、このメールを無視してください。</p>
