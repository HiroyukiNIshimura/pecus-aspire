@model Pecus.Libs.Mail.Templates.Models.PasswordSetupEmailModel
パスワード設定のお知らせ

@Model.UserName さん、アカウントが作成されました

こんにちは、@Model.UserName さん

@Model.OrganizationName の管理者によって、あなたのアカウントが作成されました。

アカウントを使用するには、まずパスワードを設定する必要があります。以下のURLをクリックして、パスワードを設定してください。

パスワード設定URL: @Model.PasswordSetupUrl

重要: このリンクは @Model.TokenExpiresAt.ToString("yyyy年MM月dd日 HH:mm") まで有効です。有効期限を過ぎると、再度パスワード設定のリクエストが必要になります。

アカウント情報:
- ユーザー名: @Model.UserName
- メールアドレス: @Model.Email
- 組織: @Model.OrganizationName
- 作成日: @Model.CreatedAt.ToString("yyyy年MM月dd日")

このメールに心当たりがない場合は、このメールを無視していただいて構いません。アカウントは作成されていても、パスワードが設定されない限りログインできません。

---
@Model.OrganizationName
このメールは自動送信されています。返信しないでください。