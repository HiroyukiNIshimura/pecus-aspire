@model Pecus.Libs.Mail.Templates.Models.OrganizationCreatedEmailModel
@{
    Layout = "_Layout.html.cshtml";
}

<h2>@Model.RepresentativeName さん、組織の登録が完了しました</h2>

<p>お疲れ様です。</p>

<p>「@Model.OrganizationName」の登録が完了いたしました。</p>

<p>システムへのアクセスを開始するには、管理者アカウントのパスワード設定を行う必要があります。以下のボタンからお進みください。</p>

<div style="text-align:center;">
    <a href="@Model.PasswordSetupUrl" class="button" style="background:#2196F3;">管理者アカウントのパスワードを設定する</a>
</div>

<div class="warning">
    <strong>重要:</strong> このリンクは @Model.TokenExpiresAt.ToString("yyyy年MM月dd日 HH:mm") まで有効です。期限内にパスワードを設定してください。
</div>

<p><strong>登録された情報:</strong></p>
<ul>
    <li><strong>組織名:</strong> @Model.OrganizationName</li>
    <li><strong>組織メールアドレス:</strong> @Model.OrganizationEmail</li>
    <li><strong>代表者名:</strong> @Model.RepresentativeName</li>
    <li><strong>管理者ユーザー名:</strong> @Model.AdminUserName</li>
    <li><strong>管理者メールアドレス:</strong> @Model.AdminEmail</li>
    <li><strong>登録日時:</strong> @Model.CreatedAt.ToString("yyyy年MM月dd日 HH:mm")</li>
</ul>

<div style="margin-top:20px; padding:15px; background:#f5f5f5; border-radius:5px;">
    <p style="margin:0; font-size:13px; color:#666;"><strong>パスワード設定について:</strong></p>
    <ul style="margin:10px 0 0 20px; font-size:13px; color:#666;">
        <li>パスワードは8文字以上100文字以内で設定してください</li>
        <li>パスワードは大文字・小文字・数字をすべて含む必要があります</li>
        <li>設定後、管理者アカウントでシステムにログインできます</li>
    </ul>
</div>

<p style="font-size:14px; color:#666; margin-top:20px;">このメールに心当たりがない場合は、このメールを無視してください。</p>
