@model Pecus.Libs.Mail.Templates.Models.WorkspaceCreatedEmailModel

新しいワークスペースが作成されました

@Model.OrganizationName に新しいワークスペースが作成されました。

ワークスペース名: @Model.WorkspaceName

@if (!string.IsNullOrWhiteSpace(Model.Description))
{
@:説明:
@:---
@:@Model.Description
@:---
@:
}
詳細:
- ワークスペースコード: @Model.WorkspaceCode
@if (!string.IsNullOrWhiteSpace(Model.CategoryName))
{
@:- カテゴリ: @Model.CategoryName
}
- 作成者: @Model.CreatedByName
- 作成日時 (JST): @Model.CreatedAt.ToString("yyyy/MM/dd HH:mm")
- 組織: @Model.OrganizationName

ワークスペースを表示: @Model.WorkspaceUrl

このメールに心当たりがない場合は、管理者またはサポートまでご連絡ください。
