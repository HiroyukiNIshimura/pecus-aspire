@model Pecus.Libs.Mail.Templates.Models.PasswordResetEmailModel

パスワードリセット

@Model.UserName 様

パスワードリセットのリクエストを受け付けました。

以下のURLをクリックして、新しいパスワードを設定してください：

@Model.PasswordResetUrl

注意：
このリンクは @Model.TokenExpiresAt.ToString("yyyy年MM月dd日 HH:mm") まで有効です。
リンクが期限切れの場合は、再度リセットリクエストを行ってください。

このリクエストに心当たりがない場合は、このメールを無視してください。

よろしくお願いいたします。

---
このメールは自動送信されています。返信しないでください。
リクエスト日時: @Model.RequestedAt.ToString("yyyy年MM月dd日 HH:mm:ss")