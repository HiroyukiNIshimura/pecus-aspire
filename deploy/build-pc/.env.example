# ビルドPC 環境変数設定

# レジストリホスト（ビルドPC自身の場合は localhost）
REGISTRY_HOST=localhost

# レジストリポート
REGISTRY_PORT=5000

# レジストリデータディレクトリ（絶対パスまたは docker-compose.registry.yml からの相対パス）
REGISTRY_DATA_DIR=./data/registry

# ビルド対象のプロジェクトルート（このファイルから見た相対パス）
PROJECT_ROOT=../..

# イメージクリーンアップの保持日数（デフォルト）
CLEANUP_DAYS=7
