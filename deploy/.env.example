# ============================================
# Pecus Aspire - Docker Compose 環境変数
# ============================================
# 【重要】このファイルは直接編集しないでください！
#
# 本番環境では以下のコマンドで自動生成されます:
#   node scripts/generate-appsettings.js -P
#
# 設定の一元管理:
#   config/settings.base.json      - 基本設定（Git管理）
#   config/settings.base.prod.json - 本番シークレット（Git管理外）
#
# このファイルはサンプルとして残しています。
# ============================================

# PostgreSQL
POSTGRES_USER=pecus
POSTGRES_PASSWORD=your_secure_password_here
POSTGRES_DB=pecusdb

# Data Path (host directory for volumes)
# 本番環境では絶対パスを指定してください
# 例: /var/pecus/data
DATA_PATH=/var/pecus/data

# Ports (external)
WEBAPI_PORT=7265
FRONTEND_PORT=3000

# URLs (public)
FRONTEND_URL=https://your-domain.com
NEXT_PUBLIC_API_URL=https://your-domain.com/api

# Docker internal hosts and URLs
LEXICAL_CONVERTER_URL=http://lexicalconverter:5100
POSTGRES_HOST=postgres
REDIS_HOST=redis
REDIS_FRONTEND_HOST=redis-frontend
WEBAPI_HOST=pecusapi
