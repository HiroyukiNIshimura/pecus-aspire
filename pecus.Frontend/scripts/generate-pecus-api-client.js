#!/usr/bin/env node

const fs = require("fs");
const path = require("path");

const PECUS_SERVICES_DIR = path.join(
  __dirname,
  "..",
  "src",
  "connectors",
  "api",
  "pecus",
  "services",
);
const OUTPUT_FILE = path.join(
  __dirname,
  "..",
  "src",
  "connectors",
  "api",
  "PecusApiClient.generated.ts",
);

function getServiceClassName(fileName) {
  return fileName.replace(".ts", "");
}

function generateFileContent(serviceClasses) {
  let content =
    "// このファイルは自動生成されます。手動で編集しないでください。\n";
  content += "// Generated by: scripts/generate-pecus-api-client.js\n\n";
  content += 'import { OpenAPI } from "./pecus/core/OpenAPI";\n';
  content += "import {\n";

  serviceClasses.forEach((className, index) => {
    content += `  ${className}`;
    if (index < serviceClasses.length - 1) content += ",";
    content += "\n";
  });

  content += '} from "./pecus";\n\n';
  content += "/**\n";
  content += " * OpenAPI 設定を初期化する関数\n";
  content += " * @param baseUrl - API のベース URL\n";
  content += " * @param getToken - トークン取得関数（オプション）\n";
  content += " */\n";
  content += "export function configureOpenAPI(\n";
  content += "  baseUrl: string,\n";
  content += "  getToken?: () => Promise<string | undefined>\n";
  content += ") {\n";
  content += "  OpenAPI.BASE = baseUrl;\n";
  content += "  OpenAPI.WITH_CREDENTIALS = true;\n";
  content += "  OpenAPI.CREDENTIALS = 'include';\n";
  content += "  \n";
  content += "  if (getToken) {\n";
  content += "    OpenAPI.TOKEN = async () => {\n";
  content += "      const token = await getToken();\n";
  content += "      return token || '';\n";
  content += "    };\n";
  content += "  }\n";
  content += "}\n\n";
  content += "/**\n";
  content += " * API サービスのインスタンスを返す関数\n";
  content += " * @returns API サービスのオブジェクト\n";
  content += " */\n";
  content += "export function createApiClientInstances() {\n";
  content += "  return {\n";

  serviceClasses.forEach((className, index) => {
    let propertyName = className.charAt(0).toLowerCase() + className.slice(1);
    if (propertyName.endsWith("Service")) {
      propertyName = propertyName.slice(0, -7);
    }
    content += `    ${propertyName}: ${className}`;
    if (index < serviceClasses.length - 1) content += ",";
    content += "\n";
  });

  content += "  };\n";
  content += "}\n";

  return content;
}

function main() {
  try {
    console.log("🔍 Scanning pecus/services directory...");

    if (!fs.existsSync(PECUS_SERVICES_DIR)) {
      console.error(`❌ Services directory not found: ${PECUS_SERVICES_DIR}`);
      console.error(
        'Please run "npm run generate:api" first to generate API client code.',
      );
      process.exit(1);
    }

    const files = fs
      .readdirSync(PECUS_SERVICES_DIR)
      .filter((f) => f.endsWith(".ts") && f !== "index.ts")
      .sort();

    console.log(`📋 Found ${files.length} Service files:`);
    files.forEach((file) => console.log(`  - ${file}`));

    const serviceClasses = files.map(getServiceClassName);
    console.log(`🏗️  Generated ${serviceClasses.length} Service class names`);

    const content = generateFileContent(serviceClasses);
    fs.writeFileSync(OUTPUT_FILE, content, "utf8");

    console.log(
      `✅ PecusApiClient.generated.ts generated successfully at: ${OUTPUT_FILE}`,
    );
    console.log(`📊 Generated ${serviceClasses.length} API service instances`);
  } catch (error) {
    console.error("❌ Error generating PecusApiClient.generated.ts:", error);
    process.exit(1);
  }
}

if (require.main === module) main();
module.exports = { main };
