/**
 * ユーザー情報型（アプリケーション全体で使用）
 * 自動生成されたUserResponseを元に、アプリケーション固有のプロパティを追加
 */
export type UserInfo = {
  id: number;
  name: string;
  email: string;
  roles: {
    id: number;
    name?: string;
  }[];
  isAdmin: boolean;
  skills?: {
    id: number;
    name?: string;
  }[];
  loginId?: string;
  username?: string;
  rowVersion?: number;
  avatarType?: 'Gravatar' | 'UserAvatar' | 'AutoGenerated';
  userAvatarPath?: string | null;
  identityIconUrl: string; // 必須（バックエンドが必ず返す）
  createdAt?: string;
};
